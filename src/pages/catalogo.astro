---
import Layout from '../layouts/Layout.astro';
import ProductCard from '../components/ProductCard.astro';
import products from '../data/products.json';
---

<Layout title="Catálogo Completo | GEMES M-3">
  
  <div class="relative bg-zinc-900 py-24 border-b border-zinc-800 overflow-hidden">
    <div class="absolute inset-0 opacity-20">
        <img src="/images/hero-gemes.jpg" class="w-full h-full object-cover grayscale" />
    </div>
    <div class="absolute inset-0 bg-gradient-to-t from-zinc-900 via-zinc-900/90 to-transparent"></div>

    <div class="relative max-w-7xl mx-auto px-4 text-center z-10">
      <h1 class="font-heading text-5xl md:text-7xl text-white uppercase mb-4 tracking-tight drop-shadow-lg">
        Nuestro <span class="text-gemes-gold">Arsenal</span>
      </h1>
      <p class="text-xl text-zinc-400 max-w-2xl mx-auto font-light">
        Herramientas de precisión para cada estilo. Desde el jazz más suave hasta el metal más pesado.
      </p>
    </div>
  </div>

  <main class="bg-black min-h-screen py-16 px-4">
    <div class="max-w-7xl mx-auto">
      
      <div class="flex flex-col md:flex-row justify-between items-center mb-12 pb-8 border-b border-zinc-800 gap-6">
        
        <p class="text-zinc-500 uppercase tracking-widest text-sm font-bold">
          Mostrando <span class="text-white">{products.length}</span> Modelos
        </p>
        
        <div class="flex flex-wrap gap-4 justify-center">
            <button class="px-6 py-2 rounded-full border border-gemes-gold bg-gemes-gold text-black font-bold text-sm uppercase hover:bg-white transition-colors">
                Todos
            </button>
            <button class="px-6 py-2 rounded-full border border-zinc-700 text-zinc-300 font-bold text-sm uppercase hover:border-gemes-gold hover:text-gemes-gold transition-colors">
                Hickory
            </button>
            <button class="px-6 py-2 rounded-full border border-zinc-700 text-zinc-300 font-bold text-sm uppercase hover:border-gemes-gold hover:text-gemes-gold transition-colors">
                Maple
            </button>
            <button class="px-6 py-2 rounded-full border border-zinc-700 text-zinc-300 font-bold text-sm uppercase hover:border-gemes-gold hover:text-gemes-gold transition-colors">
                Signature
            </button>
        </div>

        <div class="relative group">
            <select class="appearance-none bg-zinc-900 text-white border border-zinc-700 px-6 py-2 pr-10 rounded-lg focus:border-gemes-gold outline-none cursor-pointer uppercase text-sm font-bold tracking-wider">
                <option>Destacados</option>
                <option>Precio: Menor a Mayor</option>
                <option>Precio: Mayor a Menor</option>
            </select>
            <div class="absolute right-3 top-1/2 transform -translate-y-1/2 pointer-events-none text-gemes-gold">
                ▼
            </div>
        </div>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
        {products.map((product) => (
          <ProductCard 
            model={product.model}
            brand={product.brand}
            material={product.material}
            price={product.price}
            image={product.image}
            tip={product.tip} 
          />
        ))}
      </div>

      <div class="flex justify-center mt-24">
        <nav class="flex gap-3">
            <button class="w-12 h-12 flex items-center justify-center bg-gemes-gold text-black font-bold text-lg rounded hover:bg-white transition-colors">1</button>
            <button class="w-12 h-12 flex items-center justify-center border border-zinc-800 text-zinc-500 hover:text-gemes-gold hover:border-gemes-gold transition-colors rounded">2</button>
            <button class="w-12 h-12 flex items-center justify-center border border-zinc-800 text-zinc-500 hover:text-gemes-gold hover:border-gemes-gold transition-colors rounded">→</button>
        </nav>
      </div>

    </div>
  </main>

</Layout>